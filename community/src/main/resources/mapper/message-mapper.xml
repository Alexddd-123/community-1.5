<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.dao.MessageMapper">
    <sql id="selectFields">
        id, from_id, conversation_id, content, status, create_time
    </sql>

    <select id="selectConversation" resultType="Message">
/*显示私信，一个人可能会接受多个人的私信，而且两人之间互通的可能有多条私信，因此使用一个子查询
 子查询代表的是两人之间的通话，子查询进行分组查询，把相同通话的两个人的数据分在一组，并且从中筛选出最大的id，即最新的消息显示出来。
  之后外层查询根据id筛选出会话信息，并按倒叙排列，因为id越大，说明这个消息越新
  */
        select <include refid="selectFields"></include>
        from message
        where id in(
        select max(id) from message
        where status != 2
        and from_id != 1
        and (from_id = #{userId} or to_id = #{userId})
        group by conversation_id;
        )
        order by id desc
        limit #{offset},#{limit}
    </select>

    <select id="selectConversationCount" resultType="int">
        select count(m.maxid) from (
            select max(id) as maxid from message
            where status != 2
            and from_id != 1
            and (from_id = #{userId} or to_id = #{userId})
            group by conversation_id;
        ) as m
    </select>

    <
</mapper>